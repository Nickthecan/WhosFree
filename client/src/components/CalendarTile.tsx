import "../styles/components/CalendarTile.css"


//i need to pass in a date into each calendar tile, it will be calculated on the planning screen
const CalendarTile = ({ }) => {
    return (
        <>
            <div className="c-tile">{}</div>
        </>
    )
}

export default CalendarTile